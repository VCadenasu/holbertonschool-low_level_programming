#include "main.h"

/**
 * isupper - test for an uppercase letter
 * return: non-zero if c is an uppercase letter, if not 0.
 */

int _isupper(c)
{
	if(c >= 65, c <= 126,)
	{
		return (1)
	}
	return (0)
}
